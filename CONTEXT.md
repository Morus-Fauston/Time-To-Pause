# CONTEXT.md — Time To Pause（停一下吧）

> 领域术语表。本文档仅收录术语定义，不包含实现细节。

---

## 核心术语

### 短视频额度
用户观看短视频的"注意力预算"。初始值 100，观看短视频时递减，停止观看时恢复。**无每日重置**——额度通过"停止观看 → 自动恢复"的循环自然调节。归零时触发视觉干预。上限 100，下限 0。

### 消耗 & 恢复
| 时段 | 观看时（消耗） | 不观看时（恢复） |
| :--- | :--- | :--- |
| 白天 06:00-23:00 | 10 点/分钟 | 5 点/分钟 |
| 夜间 23:00-06:00 | 16 点/分钟 | 3 点/分钟 |

净速率：白天 -5 点/分钟，夜间 -13 点/分钟。即看 1 分钟约需休息 2 分钟（白天）或 5 分钟（夜间）回本。

### 检测范围
- **精准命中**：抖音、快手等纯短视频 App（包名白名单）— 事件驱动，秒级响应
- **B 站检测策略（保守）**：
  - 仅 `StoryVideoActivity` / `FeedVideoActivity` 白名单 → 消耗
  - `MainActivity`（首页混合流）→ 不消耗（宁可漏检不误伤）
  - `VideoDetailsActivity`（长视频）→ 不消耗
  - Activity 名称通过 `AccessibilityService` 实时获取
- **不检测**：微信视频号、浏览器网页版短视频等非主流入口——未来可扩展

### 后台计时架构
- **事件驱动层**：`AccessibilityService` 监听 `TYPE_WINDOW_STATE_CHANGED`——应用切换时**即时**触发，记录包名 + Activity 名 + 时间戳
- **结算层**：`ForegroundService` 内 `Handler.postDelayed(Ns)`——仅做数学结算（将累计停留秒数按费率转为额度变化），不参与检测
- **检测与结算解耦**：两条独立的时间线互不依赖。事件驱动保证即时响应，定时器保证定期结算
- **停留时长精确计算**：AccessibilityService 记录每次 enter/leave 时间戳 → 求差得到精确毫秒数 → 结算时按秒扣减
- **兜底**：AccessibilityService 未启用时，自动降级为 `UsageStatsManager` 轮询（分钟级粗粒度，无 Activity 名）

### 视觉干预蒙层
- **方案**：`WindowManager` 全屏悬浮半透明覆盖层（非系统级颜色反转）
- **技术核心**：一个 View + 纯色 alpha 背景，拦截下层点击
- **视觉选项（运行时切换）**：
  - **方案 A — 漂白模式（默认/优先实现）**：85% 纯白覆盖（`Color.argb(217, 255, 255, 255)`），画面被漂白泛灰。代码最简单，一行颜色值。
  - **方案 B — 渐变呼吸模式**：顶部 60% 透明度渐变到底部 90%，中央 ⏸️ 圆环呼吸动画（3 秒周期缩放），底部应用画面隐约可见以增强"自我觉察"。需 `ValueAnimator` 实现动画。
- **交互**：
  - 蒙层覆盖全屏，阻挡下层 App 触控
  - 中央显示 ⏸️ 图标 + "今日额度已用完，Time To Pause。" + "申请宽限"按钮
  - 悬浮球仍显示在蒙层之上，用于触发宽限答题
- **触发时机**：短视频在前台且额度归零时 → 立即覆盖
- **非短视频时**：额度为 0 也不显示蒙层，不打扰用户
- **切回短视频时**：若额度仍为 0，触发蒙层
- **悬浮球层级**：悬浮球在蒙层之上，确保用户可通过悬浮球触发宽限
- **关闭**：宽限开始或用户切出短视频 App 时自动隐藏

### 悬浮球
- **显示规则**：
  - Service 运行期间**始终显示**（让用户随时看到当前额度）
  - 蒙层显示时 → 悬浮球在蒙层之上，可点击触发宽限
  - 宽限期间 → 显示额度 + 正常恢复动画（以连续平滑动画体现时间流逝）
- **交互**：可拖动，单击弹出宽限对话框

### 后台进程恢复
- **被杀期间处理**：仅按"未在看"回溯恢复（不消耗），加载 `last_tick_time` 模拟恢复至多到 100
- **开机自启**：
  - 注册 `BOOT_COMPLETED` 广播接收器
  - 默认关闭（用户手动启动 App），可在初始化引导中开启
  - 开启后开机自动启动 `ForegroundService` 并回溯恢复

### 首次启动流
1. **欢迎页**：展示 ⏸️ 图标 + "是时候，停一下了" + 模拟进度条动效 + 三行功能介绍
2. **"开始使用"按钮** → 进入阶梯式权限引导
3. **权限引导**：`BIND_ACCESSIBILITY_SERVICE`（无障碍）→ `SYSTEM_ALERT_WINDOW`（悬浮窗）→ 电池优化（推荐不强制）
4. **开机自启选项**：默认关闭，用户可选开启
5. **完成设置** → 启动 `ForegroundService` + `AccessibilityService`，进入正常运行

### UI 技术栈
- **全量方案**：XML + View 体系（`WindowManager` 自定义 View + Activity XML 布局）
- **选型理由**：悬浮球和蒙层天然需要传统 View；VS Code 对 XML 支持优于 Compose；技术栈统一；无额外依赖
- **不使用**：Jetpack Compose（VS Code 无实时预览、悬浮窗场景不支持）

### 数据持久化
- **方案**：`SharedPreferences`
- **存储内容**：
  - `quota: Int` — 当前短视频额度（0-100）
  - `grace_remaining_secs: Long` — 宽限剩余秒数
  - `grace_end_timestamp: Long` — 宽限结束时间戳（App 被杀后恢复用）
  - `last_tick_time: Long` — 上次额度更新的时间戳
- **选型理由**：数据量极小（4 个 key-value）、无需协程/Flow、减少依赖

### 权限策略（阶梯式引导）
- **第一步（必须）**：引导开启 `BIND_ACCESSIBILITY_SERVICE` — 实时前台检测依赖（需在系统设置中搜索"无障碍"→"已安装的服务"中找到"停一下吧"并开启）
- **第二步（必须）**：引导开启 `SYSTEM_ALERT_WINDOW` — 悬浮球+蒙层依赖
- **第三步（推荐，不强制）**：引导关闭电池优化 — 后台保活
- **兜底降级模式**：若用户拒绝无障碍服务，自动 fallback 到 `PACKAGE_USAGE_STATS` 轮询（需单独引导开启）。功能降级为分钟级粗粒度、无 B 站 Activity 区分、无实时蒙层响应。

### 宽限机制
- **触发**：悬浮球单击（额度为 0 时），或蒙层"申请宽限"按钮
- **验证**：百以内加减算术题，答错换题无限重试——目的为制造"停顿反思"而非惩罚
- **时长**：5 分钟
- **额度行为**：宽限期间**额度完全冻结**——不消耗（在看也不扣），也不恢复。宽限只是"暂停干预"，不是"回血时间"。
- **叠加规则**：宽限期间无法叠加或重置
- **通知**：`ForegroundService` 通知栏显示剩余宽限倒计时
- **宽限结束**：
  - 若额度仍为 0（冻结时的值）→ 触发干预蒙层
  - 若冻结时额度 > 0 → 恢复正常消耗/恢复模式

